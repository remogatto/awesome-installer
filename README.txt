== DESCRIPTION

A simplistic rakefile that automates the building/installation/configuration process of awesome 
WM on ubuntu boxes.

== USAGE

rake -f awesome-installer.rb build_all

To list all available tasks:

rake -f awesome-installer.rb -T

